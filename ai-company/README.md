# AI社長室（AI社員型 SNS・note 売上最適化システム）

## これは何のアプリ？

Instagram・TikTok・YouTube・note・lit.link・LINEのデータを、
「AI社員」たちが分析し、「AI社長」が

**「今日、一番やるべきことは何か」**

を毎日1つだけ教えてくれるアプリです。

最終ゴールは再生数やフォロワー数を増やすことではなく、
**noteの購入数・売上を増やすこと**です。

現在は **PHASE 0（設計とダミーデータのUI試作品）** の段階です。
実際のSNSデータやAIによる自動判断はまだ接続されていません。
まずは画面のデザインと構成を確認するための試作品です。

---

## 起動方法（PHASE 0：試作品を見る）

このプロトタイプはインストール作業が不要です。

1. パソコンでこのリポジトリを開きます
2. `ai-company/prototype/index.html` というファイルを見つけます
3. そのファイルを**ダブルクリック**して、ブラウザ（Chrome・Safariなど）で開きます

これだけで、ダミーデータを使ったダッシュボード画面が表示されます。

もしダブルクリックで開けない場合は、ターミナル（黒い画面）で次のコマンドを実行してください。

```
cd ai-company/prototype
python3 -m http.server 8000
```

実行後、ブラウザで `http://localhost:8000` を開きます。

## 終了方法

- ダブルクリックで開いた場合：ブラウザのタブを閉じるだけでOKです
- `python3 -m http.server` を使った場合：ターミナルで `Ctrl + C` を押すと終了します

## 再起動方法

終了方法の手順のあと、もう一度「起動方法」を実行してください。

## API接続方法（現時点）

PHASE 0では、まだ実際のInstagramやnoteのデータには接続していません。
すべて `ai-company/prototype/app.js` の中の `dummyData` というダミーの数字です。

PHASE 1（Instagram接続）以降で、実際のAPI接続の手順（どの画面を開くか、
どこを押すか、何を入力するかまで）をこのREADMEに追記していきます。

## データの保存場所

現時点ではデータベースはまだありません。PHASE 1以降で、
このリポジトリの中に1つのファイルとして保存されるデータベース（SQLite）を
使う予定です。保存場所が決まり次第、ここに追記します。

## バックアップ

PHASE 1以降、データベースファイルができたら、
そのファイルを定期的にコピーして別の場所に保存することでバックアップできます。
具体的なバックアップ手順は、データベース実装時にこのREADMEへ追記します。

## トラブルが起きたとき

まずはご自身でコードを調べる必要はありません。
「〇〇の画面を開いたら真っ白になった」「エラーが出た」など、
見えている状況をそのまま伝えてください。原因の確認・修正はこちらで行います。

## アップデート方法（開発の進め方）

このシステムは一気に完成させず、以下の順番で段階的に作っていきます。

- PHASE 0：設計整理＋ダミーデータのUI試作品（今ここ）
- PHASE 1：Instagram接続を中心に実用レベルへ
- PHASE 2：note・lit.link・LINEを追加し、売上導線を分析できるように
- PHASE 3：TikTok・YouTubeを追加
- PHASE 4：AI社員会議・成功パターン学習・A/Bテストなど高度化

各PHASEの詳しい内容は [`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md) にまとめています。

---

## 重要な安全ルール

- AIが勝手にnoteの内容やSNSのプロフィールを書き換えることはありません。常に「提案 → あなたの確認 → 実行」という流れです。
- APIキーやログイン情報などの秘密情報は、コードに直接書かず `.env` という専用ファイルで管理します（PHASE 1以降で導入）。この`.env`ファイルはGitHubにはアップロードされない設定にします。
